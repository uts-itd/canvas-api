export function getSinglePollSubmission(pollId: string, pollSessionId: string, pollSubmissionId: string): Promise<any>;
export function createSinglePollSubmission(pollId: string, pollSessionId: string, body: any): Promise<any>;
